---
apiVersion: v1
kind: Service
metadata:
  name: portal
  labels:
    app: portal
spec:
  selector:
    app: portal
  ports:
    - name: portal-http
      protocol: TCP
      port: 5173
      targetPort: 5173
